# 本地开发 API 代理目标（vite dev 必需）
# `npm run dev` / `dev:manager` / `dev:developer` 启动时，/api 请求会被代理到该地址；
# 未设置时 vite 会拒绝启动（见 vite.config.ts）。
VITE_API_PROXY_TARGET=http://127.0.0.1:8765

# Electron 内嵌后端地址（桌面端打包后使用）
# 桌面端启动内置 FastAPI 后端时监听的地址，必须是带端口的 http:// URL；
# 未设置时跳过内嵌后端启动（窗口仍会正常打开，仅内嵌后端不可用）。
BACKEND_API_URL=http://127.0.0.1:8765
